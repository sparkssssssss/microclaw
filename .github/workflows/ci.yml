name: Build microclaw

on: [push, pull_request]

jobs:
  build:
    # 将 ubuntu-latest 修改为 ubuntu-20.04
    runs-on: ubuntu-20.04 
    
    steps:
      - uses: actions/checkout@v3
      
      # 下面是你原本的安装依赖和编译命令
      - name: Build project
        run: |
          make # 或者你的编译命令
